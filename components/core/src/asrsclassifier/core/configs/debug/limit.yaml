# @package _global_

defaults:
  - default

trainer:
  max_epochs: 3
  limit_train_batches: 0.01  # uses only 1% of the training data
  limit_val_batches: 0.05  # uses only 5% of validation data
  limit_test_batches: 0.05 # uses only 5% of test data
